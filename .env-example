# Development database

DEV_DB_NAME=your-db-name
DEV_DB_USER=homestead
DEV_DB_PASSWORD=secret
DEV_DB_HOST=127.0.0.1

# Staging database

STAGING_DB_NAME=database_name
STAGING_DB_USER=database_user
STAGING_DB_PASSWORD=database_password
STAGING_DB_HOST=database_host

# Production database

PRODUCTION_DB_NAME=database_name
PRODUCTION_DB_USER=database_user
PRODUCTION_DB_PASSWORD=database_password
PRODUCTION_DB_HOST=database_host

# Url values - Without http://

WP_DEV=example.com
WP_STAGING=example.com
WP_PRODUCTION=example.com
WP_PRODUCTION_FULL=www.example.com
